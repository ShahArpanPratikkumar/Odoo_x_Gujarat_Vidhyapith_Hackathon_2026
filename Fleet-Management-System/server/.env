PORT=5000
NODE_ENV=development

# PostgreSQL — replace with your Supabase / local DB credentials
DB_HOST=localhost
DB_PORT=5432
DB_NAME=transvora_fleet
DB_USER=postgres
DB_PASS=yourpassword

# If using Supabase, use this instead (comment the above 4 lines):
# DATABASE_URL=postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres

# JWT
JWT_SECRET=transvora_super_secret_jwt_key_2026
JWT_EXPIRES_IN=7d

# CORS — Vite dev server
CORS_ORIGIN=http://localhost:5173
